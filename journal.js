// 1. 
function greet(name) {
  return "Hello, " + name + "!";
}

// 2. 
function add(a, b) {
  return a + b;
}

// 3.
function multiply(a, b) {
  return a * b;
}

// 4. 
function isEven(n) {
  return n % 2 === 0;
}

// 5. Reverse 
function reverseString(str) {
  return str.split("").reverse().join("");
}
